---
import { TESTIMONIALS } from "../../../data/human-design.constants.ts";

const { eyebrow = "Letters from clients", eyebrowEdit = {} } = Astro.props;
---

<section class="client-letters-section" data-client-letters-section>
  <p class="letters-eyebrow" {...eyebrowEdit}>{eyebrow}</p>
  <div class="letters-grid">
    {TESTIMONIALS.map((t, i) => (
      <figure class={`testimonial-card test-card-${i + 1}`}>
        <blockquote class="testimonial-quote">
          {t.quote}
        </blockquote>
        <figcaption class="testimonial-author">
          {t.author}
        </figcaption>
      </figure>
    ))}
  </div>
</section>

<style>
  .client-letters-section {
    margin-top: 104px;
    background: #0B3D2E;
    color: #17191A;
    padding: 64px 32px;
  }

  .letters-eyebrow {
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 0.26em;
    text-transform: uppercase;
    color: #5C2B2E;
    margin: 0 0 44px;
  }

  .letters-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
    align-items: start;
  }

  .testimonial-card {
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .test-card-1 { padding-top: 0; }
  .test-card-2 { padding-top: 40px; }
  .test-card-3 { padding-top: 80px; }

  .testimonial-quote {
    font-family: var(--font-display);
    font-weight: 300;
    font-size: clamp(17px, 1.7vw, 23px);
    line-height: 1.3;
    letter-spacing: -0.04em;
    margin: 0;
    text-wrap: pretty;
  }

  .testimonial-author {
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #6E7169;
  }

  @media (max-width: 900px) {
    .letters-grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }
    .test-card-2, .test-card-3 {
      padding-top: 0;
    }
  }

  @media (max-width: 640px) {
    .client-letters-section {
      padding: 48px 20px;
    }
  }

  /* Reading Almanac theme */
  .client-letters-section { margin-top: 0; background: #FFFFFF; color: #17191A; border-block: 1px solid #E8E7E1; }
  .letters-grid { border-top: 1px solid #D8D7D0; padding-top: 34px; }
  .testimonial-card { border-right: 1px solid #E8E7E1; padding-right: 34px; }
  .testimonial-card:last-child { border-right: 0; }
  .testimonial-author { color: #8B8E86; }
</style>
